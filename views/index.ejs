<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Monopoly</h1>
    <ul>
        <% for(let i =0; i < monopoly.length; i++){ %>
            <div class="cardText">
                <a href="/monopoly/<%= monopoly[i]._id%>">
                    <button>
                    <%=monopoly[i].name%>
                </button>
                </a>
                <p>Piece: <%=monopoly[i].piece%></p>
                <p>Money: <%=monopoly[i].money%></p>
                <p>Properties: </p>
                    <% if (monopoly[i].properties.length === 0) { %>
                        <p>No Properties</p>
                    <% } %>
                <ul>
                <% for(let j =0; j < monopoly[i].properties.length; j++){ %>
                        <li>
                            <%=monopoly[i].properties[j].property.propName%>
                        </li>
                    <%}%>
                    </ul>
            </div>
            <div class="editBtn">
                <a href="/monopoly/<%=monopoly[i]["_id"] %>/edit">
                    <button>Edit</button>
                </a>
            </div>
            <form action="/monopoly/<%=monopoly[i]["_id"]; %>?_method=DELETE" method="POST">
                <input type="submit" value="DELETE"/>
            </form>
        <% } %>
    </ul>
</body>
</html>